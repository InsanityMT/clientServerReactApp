import React from 'react'
import ReactLoading from 'react-loading'

const Loading = () => {
    return <ReactLoading type='spinningBubbles' width={400} height={400} color='#000' />
}

export default Loading